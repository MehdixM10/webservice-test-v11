# Run

1. First rename .env.example to .env and fill up the variables

```bash
    mv .env.example .env
```

2. Then run it using :

- bun :

```bash
    bun run --env-file=.env .\index.ts
```

- or Node :

```bash
    bun run --env-file=.env .\index.ts
```
